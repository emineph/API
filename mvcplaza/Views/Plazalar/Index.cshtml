@model IEnumerable<mvcplaza.Models.Plazalar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "EY")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlazaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bolum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlazaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bolum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.PlazaNo  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.PlazaNo  })
        </td>
    </tr>
}

</table>
